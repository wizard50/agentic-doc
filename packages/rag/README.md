# agentic-doc-rag

RAG retrieval layer for **agentic-doc** — chunking, embeddings, hybrid search, and vector store abstractions.

## Status

WIP scaffold — currently provides RAG-specific settings (`RagSettings`). Ingestion, retrieval, and vector store implementations are not yet included.

## Layout

```
src/agentic_doc_rag/
  config.py    # Vector store and collection settings
```