# studio

M2 portfolio app — **Doc Agent** Streamlit UI for agentic documentation workflows.

Scaffold only; full UI lands in follow-up steps. From the workspace root:

```bash
uv sync --dev
uv run studio
```

Requires an indexed corpus (`uv run explorer ingest`) and `LLM_API_KEY` for live Answer runs.
