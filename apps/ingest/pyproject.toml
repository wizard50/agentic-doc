[project]
name = "ingest"
version = "0.1.0"
description = "Document ingestion application for the Agentic RAG system"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "agentic-doc-core",
    "agentic-doc-rag",
]
